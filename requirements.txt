# Streamlit Community Cloud reads this file. The app (streamlit_app.py) reads the
# committed results/toy_public.json directly, so it only needs streamlit. The
# CLI/package under robust_siting_lab/ has its own (zero) deps in pyproject.toml.
streamlit>=1.40
