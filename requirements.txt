# Streamlit Community Cloud reads this file. The app (streamlit_app.py) reads the
# committed results/toy_public.json directly AND imports the real solver from the
# robust_siting_lab package to re-solve user-edited scenarios live. The "." line
# installs this repo as a package so `import robust_siting_lab` works on the
# cloud. The package itself has zero runtime deps (see pyproject.toml).
streamlit>=1.40
.
